use std::path::{Path, PathBuf};
use std::sync::LazyLock;

use anyhow::Result;
use nn::{Activation, LossFunction, Matrix, NeuralNetwork, Optimization, matrix};

fn main() -> Result<()> {
    let p = get_saved(std::env::current_exe()?.parent().unwrap());
    let mut nn = nn_new(&p)?;
    nn.train(100, &*INPUTS, &*OUTPUTS);
    nn_test(&mut nn);
    nn.save(p)?;

    Ok(())
}

fn get_saved(parent_dir: impl AsRef<Path>) -> PathBuf {
    parent_dir.as_ref().join("nn-mnist.nn")
}

pub fn nn_new(saved: impl AsRef<Path>) -> Result<NeuralNetwork> {
    let nn = if !saved.as_ref().exists() {
        println!("Found no saved neural network in executable dir");
        let mut nn = NeuralNetwork::new(
            [
                (784, Activation::Sigmoid), // input size (28x28 pic)
                (64, Activation::Sigmoid),  // hidden layer 1
                (10, Activation::Softmax),  // output size (0-9)
            ],
            LossFunction::MeanSquaredError,
            // Optimization::NONE,
            Optimization::rmsprop(0.01, 0.90, 1e-8),
        );

        nn.train_until(0.00001, &*INPUTS, &*OUTPUTS);
        nn
    } else {
        println!("Using saved neural network");
        NeuralNetwork::load(&saved)?
    };

    Ok(nn)
}

pub fn nn_test(nn: &mut NeuralNetwork) {
    for (input, output) in (*TEST_INPUT).iter().zip(&*TEST_OUTPUT) {
        let guess = nn.guess(&input);
        let loss = LossFunction::MeanSquaredError.loss(&guess, output);
        println!("Guess: {guess}Correct: {output}Loss: {loss:7.5}\n");
    }
}

/// https://github.com/makeyourownneuralnetwork/makeyourownneuralnetwork
static INPUTS: LazyLock<Vec<Matrix>> = LazyLock::new(|| {
    vec![
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            112 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            94 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            156 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            183 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            74 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            55 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            119 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            181 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            239 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            212 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            194 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            102 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            97 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            184 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            101 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            103 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            130 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            27 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            39 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            214 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            33 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            89 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
    ]
});

static OUTPUTS: LazyLock<Vec<Matrix>> = LazyLock::new(|| {
    vec![
        matrix![5 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![2 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![5 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![2 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![2 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![2 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![5 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![5 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![5 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![2 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![1 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![2 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![9 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![4 , @ 10 ],
        matrix![6 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![0 , @ 10 ],
        matrix![7 , @ 10 ],
        matrix![8 , @ 10 ],
        matrix![3 , @ 10 ],
        matrix![1 , @ 10 ],
    ]
});

static TEST_INPUT: LazyLock<Vec<Matrix>> = LazyLock::new(|| {
    vec![
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            83 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            207 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            125 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            52 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            122 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            76 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            143 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            117 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            123 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            87 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            82 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            159 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            205 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            110 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            202 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            140 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            230 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            62 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            71 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            4 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            114 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            247 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            141 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            106 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            41 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            84 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            179 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            81 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            1 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            203 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            73 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            248 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            58 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            111 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            60 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            105 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            196 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            26 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            107 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            192 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            77 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            220 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            189 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            173 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            96 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            163 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            197 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            65 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            44 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            34 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            160 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            157 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            204 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            154 as f64 / 255 as f64,
            29 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            61 as f64 / 255 as f64,
            190 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            224 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            19 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            235 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            144 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            31 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            24 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            79 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            193 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            98 as f64 / 255 as f64,
            219 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            80 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            182 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            155 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            135 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            86 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            237 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            236 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            185 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            216 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            145 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            134 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            68 as f64 / 255 as f64,
            158 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            161 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            88 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            226 as f64 / 255 as f64,
            18 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            48 as f64 / 255 as f64,
            245 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            115 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            172 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            165 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            186 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            14 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            17 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            129 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            47 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            217 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            215 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            35 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            63 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            213 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            170 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            57 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            238 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            69 as f64 / 255 as f64,
            20 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            206 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            30 as f64 / 255 as f64,
            2 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            177 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            132 as f64 / 255 as f64,
            10 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            15 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            92 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            174 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            127 as f64 / 255 as f64,
            49 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            147 as f64 / 255 as f64,
            91 as f64 / 255 as f64,
            121 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            42 as f64 / 255 as f64,
            85 as f64 / 255 as f64,
            28 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            139 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            168 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            53 as f64 / 255 as f64,
            218 as f64 / 255 as f64,
            222 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            124 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            67 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            120 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            51 as f64 / 255 as f64,
            164 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            50 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            188 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            148 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            167 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            23 as f64 / 255 as f64,
            180 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            221 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            93 as f64 / 255 as f64,
            149 as f64 / 255 as f64,
            22 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
        matrix![
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            36 as f64 / 255 as f64,
            56 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            201 as f64 / 255 as f64,
            199 as f64 / 255 as f64,
            95 as f64 / 255 as f64,
            37 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            45 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            151 as f64 / 255 as f64,
            6 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            46 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            166 as f64 / 255 as f64,
            133 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            229 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            104 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            234 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            187 as f64 / 255 as f64,
            142 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            191 as f64 / 255 as f64,
            40 as f64 / 255 as f64,
            198 as f64 / 255 as f64,
            246 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            126 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            128 as f64 / 255 as f64,
            11 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            43 as f64 / 255 as f64,
            70 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            21 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            72 as f64 / 255 as f64,
            243 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            32 as f64 / 255 as f64,
            116 as f64 / 255 as f64,
            225 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            5 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            75 as f64 / 255 as f64,
            240 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            223 as f64 / 255 as f64,
            109 as f64 / 255 as f64,
            138 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            178 as f64 / 255 as f64,
            169 as f64 / 255 as f64,
            210 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            231 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            232 as f64 / 255 as f64,
            38 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            9 as f64 / 255 as f64,
            175 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            253 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            251 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            252 as f64 / 255 as f64,
            171 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            136 as f64 / 255 as f64,
            195 as f64 / 255 as f64,
            176 as f64 / 255 as f64,
            146 as f64 / 255 as f64,
            153 as f64 / 255 as f64,
            200 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            150 as f64 / 255 as f64,
            16 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            162 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            99 as f64 / 255 as f64,
            3 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            118 as f64 / 255 as f64,
            250 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            90 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            100 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            211 as f64 / 255 as f64,
            7 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            54 as f64 / 255 as f64,
            241 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            242 as f64 / 255 as f64,
            59 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            131 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            244 as f64 / 255 as f64,
            64 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            13 as f64 / 255 as f64,
            249 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            152 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            12 as f64 / 255 as f64,
            228 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            208 as f64 / 255 as f64,
            8 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            78 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            66 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            209 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            254 as f64 / 255 as f64,
            137 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            227 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            233 as f64 / 255 as f64,
            25 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            113 as f64 / 255 as f64,
            255 as f64 / 255 as f64,
            108 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64,
            0 as f64 / 255 as f64
        ],
    ]
});

static TEST_OUTPUT: LazyLock<Vec<Matrix>> = LazyLock::new(|| {
    vec![
        matrix![7, @ 10],
        matrix![2, @ 10],
        matrix![1, @ 10],
        matrix![0, @ 10],
        matrix![4, @ 10],
        matrix![1, @ 10],
        matrix![4, @ 10],
        matrix![9, @ 10],
        matrix![5, @ 10],
        matrix![9, @ 10],
    ]
});
